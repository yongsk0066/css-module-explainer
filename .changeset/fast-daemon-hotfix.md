---
"css-module-explainer": patch
---

Run the packaged Rust selected-query backend through a long-lived daemon by default, keeping LSP hover, reference, and diagnostics paths off per-query process spawning.
